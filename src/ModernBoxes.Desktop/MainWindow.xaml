<Window
    x:Class="ModernBoxes.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:ModernBoxes.Presentation.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="window"
    Title="MainWindow"
    DataContext="{Binding MainViewModel, Source={StaticResource Locator}}"
    AllowsTransparency="True"
    Background="Transparent"
    ResizeMode="NoResize"
    SizeToContent="Width"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    mc:Ignorable="d">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Presentation/Resources/MainWindowResource.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <hc:TransitioningContentControl>
        <Border Background="{DynamicResource MaterialBackground}" SnapsToDevicePixels="True">
            <Grid HorizontalAlignment="Left">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="1">
                    <hc:Card
                        Name="MiddleWindow"
                        Width="{Binding ElementName=Toggle, Path=IsChecked, Converter={StaticResource toconvert}}"
                        hc:BorderElement.CornerRadius="8"
                        Background="{DynamicResource RegionBrush}"
                        Effect="{StaticResource EffectShadow1}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto" />
                                <RowDefinition />
                                <RowDefinition Height="auto" />
                            </Grid.RowDefinitions>
                            <local:UCSearch Grid.Row="0" />
                            <StackPanel Grid.Row="1" Orientation="Vertical">
                                <ToggleButton
                                    Name="Toggle"
                                    Height="40"
                                    Margin="0,16,0,0"
                                    VerticalAlignment="Top"
                                    Style="{DynamicResource MainMenuToggleButton}">
                                    <ToggleButton.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="层级">
                                                <MenuItem
                                                    Name="topItem"
                                                    Click="topItem_Click"
                                                    Header="置顶"
                                                    IsCheckable="True"
                                                    IsChecked="False" />
                                                <MenuItem
                                                    Name="BottomItem"
                                                    Click="BottomItem_Click"
                                                    Header="置底"
                                                    IsCheckable="True"
                                                    IsChecked="True" />
                                            </MenuItem>
                                            <MenuItem
                                                Name="btn_fixed"
                                                Click="MenuItem_Click_To_Fixed"
                                                Header="固定"
                                                IsChecked="False" />
                                            <MenuItem
                                                Name="btn_close"
                                                Click="MenuItem_Click_To_CloseWindow"
                                                Header="关闭" />
                                        </ContextMenu>
                                    </ToggleButton.ContextMenu>
                                </ToggleButton>

                                <hc:ScrollViewer Orientation="Vertical">
                                    <ItemsControl Name="items" ItemsSource="{Binding MenuList}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Vertical" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>

                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Margin="0,6,0,0" Orientation="Horizontal">
                                                    <RadioButton
                                                        Width="200"
                                                        HorizontalAlignment="Center"
                                                        VerticalContentAlignment="Center"
                                                        Command="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ItemsControl}, Path=DataContext.OpenApp}"
                                                        CommandParameter="{Binding Target}"
                                                        Content="{Binding MenuName}"
                                                        GroupName="menu"
                                                        Style="{StaticResource MenuStyle}"
                                                        ToolTip="{Binding MenuName}" />
                                                    <StackPanel.ContextMenu>
                                                        <ContextMenu>
                                                            <MenuItem
                                                                Name="AddCardApp"
                                                                Click="AddCardApp_Click"
                                                                Header="添加卡片应用"
                                                                Visibility="{Binding MenuName, Converter={StaticResource IsShowContextMenu}}" />
                                                            <MenuItem
                                                                Name="BtnDeleteMenuItem"
                                                                Click="BtnDeleteMenuItem_Click"
                                                                CommandParameter="{Binding MenuName}"
                                                                Header="删除" />
                                                        </ContextMenu>
                                                    </StackPanel.ContextMenu>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </hc:ScrollViewer>
                                <Button
                                    Margin="0,10,0,10"
                                    HorizontalAlignment="Center"
                                    Command="{Binding AddMenuDialog}"
                                    FontFamily="/Resource/#iconfont"
                                    FontSize="25"
                                    Foreground="DarkGray"
                                    ToolTip="添加">
                                    <Button.Template>
                                        <ControlTemplate>
                                            <TextBlock Text="&#xe7c4;" />
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Opacity" Value=".9" />
                                                </Trigger>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter Property="Opacity" Value="0.4" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </StackPanel>

                            <StackPanel Grid.Row="2">
                                <Button
                                    Name="btnConfigMenu"
                                    Margin="8"
                                    HorizontalAlignment="Center"
                                    Command="{Binding OpenSetDialog}"
                                    FontFamily="/Resource/#iconfont"
                                    FontSize="20"
                                    Foreground="Gray"
                                    Style="{DynamicResource BtnConfigMenuStyle}" />
                            </StackPanel>
                        </Grid>
                    </hc:Card>
                </Grid>

                <hc:TransitioningContentControl
                    Name="componentLayoutLeft"
                    Grid.Column="0"
                    Width="440"
                    Margin="10,0,10,10"
                    Visibility="Hidden">
                    <ContentControl x:Name="LeftContent" />
                </hc:TransitioningContentControl>

                <hc:TransitioningContentControl
                    Name="componentLayoutRight"
                    Grid.Column="2"
                    Width="440"
                    Margin="10,0,10,10"
                    Visibility="Hidden">
                    <ContentControl x:Name="RightContent" />
                </hc:TransitioningContentControl>

                <hc:NotifyIcon
                    x:Name="NotifyIconContextContent"
                    Icon="/Resource/logo.ico"
                    Text="木函"
                    Visibility="Visible">
                    <hc:NotifyIcon.ContextMenu>
                        <ContextMenu>
                            <MenuItem
                                x:Name="trayShowHide"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Click="ShowHide_Click"
                                Header="隐藏主窗口" />
                            <MenuItem
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Click="QuickNote_Click"
                                Header="快速添加便签" />
                            <Separator />
                            <MenuItem
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Click="Backup_Click"
                                Header="备份数据" />
                            <MenuItem
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Click="Restore_Click"
                                Header="恢复数据" />
                            <Separator />
                            <MenuItem
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Click="Settings_Click"
                                Header="设置" />
                            <MenuItem
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Command="hc:ControlCommands.ShutdownApp"
                                FontFamily="/Resource/#iconfont"
                                Header="退出程序"
                                Icon="&#xe600;" />
                        </ContextMenu>
                    </hc:NotifyIcon.ContextMenu>
                    <hc:Interaction.Triggers>
                        <hc:EventTrigger EventName="Click">
                            <hc:EventToCommand Command="hc:ControlCommands.PushMainWindow2Top" />
                        </hc:EventTrigger>
                    </hc:Interaction.Triggers>
                </hc:NotifyIcon>
            </Grid>
        </Border>
    </hc:TransitioningContentControl>
</Window>
